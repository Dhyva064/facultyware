<!doctype html>
<html lang="id">
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="Content-Language" content="id" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><%= pageTitle || "SIMAINT Dashboard" %></title>
    <link rel="icon" type="image/svg+xml" href="/assets/favicon.svg" />
    <link rel="stylesheet" href="/assets/styles.css" />
  </head>
  <body class="bg-background text-foreground">
    <div class="flex min-h-screen flex-col bg-muted/30 lg:flex-row">
      <aside class="border-b bg-card px-5 py-6 lg:min-h-screen lg:w-72 lg:border-b-0 lg:border-r">
        <div class="flex h-full flex-col gap-6">
          <a href="/home" class="flex items-center gap-3 rounded-xl border bg-background px-4 py-3">
            <div class="flex h-10 w-10 items-center justify-center rounded-lg bg-primary text-sm font-bold text-primary-foreground">
              S
            </div>
            <div class="min-w-0">
              <p class="truncate text-sm font-semibold">SIMAINT FTI</p>
              <p class="truncate text-xs text-muted-foreground"><%= roleLabel || "Dashboard" %></p>
            </div>
          </a>

          <nav class="space-y-2 text-sm">
            <a href="/home" class="flex items-center rounded-lg px-3 py-2 font-medium hover:bg-muted">
              Dashboard
            </a>
            <a href="/logout" class="flex items-center rounded-lg px-3 py-2 font-medium hover:bg-muted">
              Logout
            </a>
          </nav>

          <div class="mt-auto rounded-xl border bg-background p-4 text-sm">
            <p class="font-semibold"><%= user %></p>
            <p class="mt-1 text-muted-foreground"><%= userEmail %></p>
            <p class="mt-3 inline-flex rounded-full bg-muted px-2.5 py-1 text-xs font-medium">
              <%= roleLabel %>
            </p>
          </div>
        </div>
      </aside>

      <div class="flex min-w-0 flex-1 flex-col">
        <header class="border-b bg-card px-5 py-4 lg:px-8">
          <div class="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between">
            <div>
              <p class="text-xs font-semibold uppercase tracking-[0.24em] text-muted-foreground">
                SIMAINT Dashboard
              </p>
              <h1 class="mt-1 text-2xl font-bold tracking-tight">
                Selamat datang, <%= user %>
              </h1>
              <p class="mt-1 text-sm text-muted-foreground">
                <%= roleSummary || "Dashboard sesuai role pengguna." %>
              </p>
            </div>
            <div class="flex items-center gap-3">
              <span class="inline-flex rounded-full border bg-muted px-3 py-1 text-sm font-medium">
                <%= roleLabel %>
              </span>
              <a href="/logout" class="inline-flex rounded-md border px-4 py-2 text-sm font-medium hover:bg-muted">
                Logout
              </a>
            </div>
          </div>
        </header>

        <main class="flex-1 p-5 lg:p-8">
          <section class="mb-6 rounded-2xl border bg-card p-6 shadow-sm">
            <div class="flex flex-col gap-2">
              <p class="text-sm font-medium text-muted-foreground">Role aktif</p>
              <h2 class="text-xl font-semibold"><%= roleLabel %></h2>
            </div>
          </section>

          <%- include((typeof dashboardView !== "undefined" && dashboardView) ? dashboardView : "pengguna/index") %>
        </main>
      </div>
    </div>
  </body>
</html>
